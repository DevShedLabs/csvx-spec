# Examples

The examples are canonical JSON resources representing the contents of CSVX ZIP packages. They
are kept unpacked so they are reviewable in source control; a package writer should place the
resources into a ZIP archive with the paths described in `spec/01-container.md`.

Each example directory is a named fixture. The `.csvx` extension is reserved for the generated ZIP
package and is not committed yet.
